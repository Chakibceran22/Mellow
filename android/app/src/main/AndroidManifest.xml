<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required on Android 13+ to show the playback notification / lock-screen controls.
         FOREGROUND_SERVICE + FOREGROUND_SERVICE_MEDIA_PLAYBACK + the playback <service>
         are merged in automatically from @rntp/player's own manifest. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Reading the on-device music library via MediaStore. READ_MEDIA_AUDIO is the
         scoped audio permission on Android 13+ (API 33); older devices fall back to
         the broad storage read, which is unused from 33 onward. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
      android:name="android.permission.READ_EXTERNAL_STORAGE"
      android:maxSdkVersion="32" />

    <!-- Picking a custom playlist cover image. On Android 13+ the system Photo
         Picker needs no runtime grant, but READ_MEDIA_IMAGES covers the
         ACTION_GET_CONTENT fallback; older devices reuse the storage read above. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
